// File: src/config.js

// Base URL for optional backend or API (we might not use it for now)
export const API_BASE = "";

// Centralized YouTube Data API key
export const YOUTUBE_API_KEY = import.meta.env.VITE_YOUTUBE_API_KEY;
